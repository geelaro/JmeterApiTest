# ==========================================
# ApiTest Build Configuration
# 复制此文件为 build.properties 并修改为你本地的路径
# Copy to build.properties and adjust paths
# ==========================================

# JMeter 安装目录 (required)
jmeter.home=D:/Lib/apache-jmeter-5.6

# 项目根目录 (optional, 默认使用 build.xml 所在目录)
# jmeter.project=.
